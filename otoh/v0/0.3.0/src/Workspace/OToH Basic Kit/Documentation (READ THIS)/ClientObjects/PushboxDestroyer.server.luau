--[==[
	Pushbox Destroyers destroy pushboxes that touch them.
	
	Attributes:
		- Id: When `PushboxRequireMatchingId` is true, pushboxes must belong to this ID group to be destroyed.
		- PushboxRequireMatchingId: See above.
]==]